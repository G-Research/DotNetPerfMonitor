name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - dashboard-setup

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up NodeJS
        uses: actions/setup-node@v2
        with:
          node-version: 16

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install Dependencies and Generate Static Content
        working-directory: ./dashboard/DotNetPerfMonitor/
        run: |
          pnpm install --frozen-lockfile --ignore-scripts
          pnpm run generate

      - name: Check GitHub Pages status
        uses: crazy-max/ghaction-github-status@v3
        with:
          pages_threshold: major_outage

      - name: Deploy to GitHub Pages 🚀
        uses: crazy-max/ghaction-github-pages@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          build_dir: ./dashboard/DotNetPerfMonitor/dist
          target_branch: dashboard-deployment
          commit_message: ${{ github.event.head_commit.message }}
        # with:
        #   token: ${{ secrets.GITHUB_TOKEN }}
        #   branch: dashboard-deployment
        #   folder: dashboard/DotNetPerfMonitor/dist
        #   commit-message: ${{ github.event.head_commit.message }}
        #   git-config-name: "github-actions[bot]"
        #   git-config-email: "github-actions[bot]@users.noreply.github.com"
